import React from 'react';
import styles from './WheelLoader.module.scss'

const WheelLoader: React.FC = () => {
    return (
        <div className={styles['wheel-loader']}/>
    );
};

export default WheelLoader;
